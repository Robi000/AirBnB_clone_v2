<!DOCTYPE html>
<HTML lang="en">

<HEAD>
    <TITLE>HBNB</TITLE>
</HEAD>

<BODY>
    <H1>State: {{state[0][1]}}</H1>
    <H3>Cities:</H3>
    <UL>
        {% if state %}
            {% for x, y in state[0][2] %}
            <LI>{{y}}: <B>{{x}}</B></LI>
            {% endfor %}
        {% else %}
        <H1>Not found!</H1>
        {% endif %}

    </UL>
</BODY>

</HTML>